function showText() {
    let hiddenElement = document.getElementById('text');
    hiddenElement.style.display = ('inline');

    let unwantedElements = document.getElementById('more');
    unwantedElements.style.display = ('none');
}